# 봉인(HMAC) 시크릿. 외부 서비스 키가 아니라 로컬 데모용 상수다.
# .env.local 로 복사해 쓰거나, 지정하지 않으면 아래 fallback 이 그대로 사용된다.
SEAL_SECRET=dev-seal-secret-synthetic-demo

# 이벤트 로그 SQLite 파일 경로 (기본값: data/demo.db)
# ANSWER_REGISTRY_DB=data/demo.db
